import axios from "axios";

const btn_check_id = document.getElementById('check-id');
const btn_check_nick = document.getElementById('check-nick');
const check_id = document.getElementsByName('check_id');
const check_nick = document.getElementsByName('check_nick');

btn_check_id.addEventListener('click', () => {
    axios.
    modal_id.style.display = 'none';
});

btn_check_nick.addEventListener('click', () => {
    modal_nick.style.display = 'none';
});

